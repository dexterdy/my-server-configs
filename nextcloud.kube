[Unit]
Description=quadlet that starts the nextcloud kubernetes pod

[Kube]
AutoUpdate=registry
Yaml=/${HOME}/nextcloud-app/nextcloud.yaml
SetWorkingDirectory=yaml

[Service]
Restart=always

[Install]
WantedBy=multi-user.target