[Unit]
Description=quadlet that starts the adguard pod

[Kube]
AutoUpdate=registry
Yaml=%h/adguard-app/adguard.yaml
SetWorkingDirectory=yaml
Network=pasta:--dns-forward,1.1.1.1

[Service]
Restart=always

[Install]
WantedBy=multi-user.target
